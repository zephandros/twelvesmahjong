{
  "name": "twelves-mahjong",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "description": "RIICHI · 咲 — single-player offline riichi mahjong (Saki Portable homage)",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vite-plugin-pwa": "^0.21.1",
    "vitest": "^3.2.4"
  }
}
